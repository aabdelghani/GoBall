# Backend has full read/write access
user backend
topic readwrite goball/#

# The shared "goball" user can publish to any device topic
# (In production, provision per-device users with: pattern write goball/%u/#)
user goball
topic write goball/#
topic read goball/#
